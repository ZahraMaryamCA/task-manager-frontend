
  # Task Management UI Design

  This is a code bundle for Task Management UI Design. The original project is available at https://www.figma.com/design/9m9e8tOs5P1gLMKua4j6ZD/Task-Management-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  